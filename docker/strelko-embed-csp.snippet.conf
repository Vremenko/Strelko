# Dodatek za StormAPI/docker/strelko-nginx.conf (zahteva ločeno potrditev / deploy nginx).
# Omogoči frame-ancestors samo za javne embed poti; ostale SPA poti ostanejo ne-frameable.

# Pred location / { ... } vstavi:

# location ^~ /embed/ {
#     add_header Content-Security-Policy "frame-ancestors https://meteoinfo.si https://www.meteoinfo.si" always;
#     add_header X-Frame-Options "" always;
#     try_files $uri $uri/ /index.html;
#     add_header Cache-Control "no-cache, no-store, must-revalidate" always;
# }
#
# location / {
#     add_header Content-Security-Policy "frame-ancestors 'self'" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     try_files $uri $uri/ /index.html;
#     add_header Cache-Control "no-cache, no-store, must-revalidate" always;
# }
#
# Opomba: pri več add_header v nested location nginx podeduje samo iz istega bloka.
# Za /arhiv/public/embed in map-embed.html CSP že nastavi strele2 (frame-ancestors strelko + meteoinfo).
