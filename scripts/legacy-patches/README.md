# Zastareli patch skripti (dist-first obdobje)

Te skripte so **zastarele** od prehoda na normalen `src/` + `npm run build`.

Ne zaganjaj jih na `dist/` — spremembe delaj v ustreznih modulih pod `src/`.

Obdržene samo za referenco (kaj je bilo kdaj v produkcijskem bundle-u).
