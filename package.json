{
  "name": "strelko",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'Vite dev onemogočen — glej RECOVERY.md. Produkcija = dist/' && exit 1",
    "build": "echo 'npm run build prepisuje dist/ — prepovedano. Glej RECOVERY.md' && exit 1",
    "preview": "echo 'Vite preview onemogočen — glej RECOVERY.md' && exit 1"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
